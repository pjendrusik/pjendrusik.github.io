# Personal Landing Page

## Update content
- Edit `index.md` for copy and sections.
- Replace `assets/cv.pdf` with the latest CV.

## Deploy
Push to the default branch and GitHub Pages will publish automatically.

## Optional local preview
If Jekyll is installed:

```sh
bundle exec jekyll serve
```
